node_modules
dist
.env
.DS_Store

# TypeScript incremental build state
tsconfig.tsbuildinfo
*.tsbuildinfo

# macOS Finder / Spotlight shadow files emitted by some xattr-aware
# copy operations (e.g. `cp -p` between volumes that don't share
# extended attributes). They have no source-of-truth value.
.!*

# Generated by the LD a11y dev-server plugin (latest violation snapshot)
.ld-a11y-report.json
