CLOSURE SUMMARY
═══════════════════════════════════════════════════════════
Offer ID:       {{ offer_id | default(external_ref) }}
Publish Status: {{ publish_status | default("UNKNOWN") }}
Audit History:  {{ audit_history | default("N/A") }}
Issue Summary:  Manual review required — automated verdict inconclusive

Reason Codes Validated: {{ reason_codes_checked | default([]) | join(", ") }}

Analysis:
Automated validation for Offer {{ offer_id | default(external_ref) }} could not produce
a definitive verdict for one or more reason codes. Insufficient catalog data or API
limitations prevented a clear determination of whether the unpublish is correctly applied.

Affected Component: Catalog Compliance — Offer Publish Status
Impact: Offer's correctness of suppression undetermined
Root Cause: Data gaps, API limitations, or edge cases in reason code validation

Recommended Actions:
  1. Manually investigate each reason code flagged for review using the diagnostic details.
  2. Retry automated validation once the underlying data issue is resolved.
  3. Escalate to the catalog engineering team if manual review cannot resolve the issue.

Status: NEEDS_MANUAL_REVIEW
Slack Summary: 🛠️ *NEEDS MANUAL REVIEW* — Offer {{ offer_id | default(external_ref) }} | Manual investigation required
👉 Manual review required: One or more reason codes need human investigation
═══════════════════════════════════════════════════════════
