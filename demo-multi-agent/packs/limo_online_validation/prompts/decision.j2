You are DecisionAgent (not invoked at runtime — decision is deterministic).

The runtime maps DiagnosticAgent's observations to a runbook card via
the ``DIAG-DECISION-MATRIX`` tool in ``tools.yaml``.  This prompt is
kept only so the pack manifest validates uniformly across packs.
