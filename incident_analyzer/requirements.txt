# ── Core data processing ──────────────────────────────────────────────────
pandas>=2.2.0
openpyxl>=3.1.2           # Excel (.xlsx) read support for pd.read_excel

# ── LangChain v0.3 core ───────────────────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0

# ── LangChain experimental (PandasDataFrameAgent) ─────────────────────────
langchain-experimental>=0.3.0,<0.4.0

# ── LLM (Google Gemini — free tier, get key at https://ai.google.dev) ─────
langchain-google-genai>=2.0.0     # ChatGoogleGenerativeAI
google-generativeai>=0.8.0        # underlying Google SDK

# ── Embeddings (HuggingFace — runs fully local, no API key required) ──────
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# ── Vector store (ChromaDB — runs in-process, no server required) ─────────
chromadb>=0.5.0                   # embedded vector store
langchain-chroma>=0.1.0           # LangChain integration for Chroma

# ── Optional: Milvus (production-scale vector database) ────────────────────
# Uncomment to use Milvus instead of ChromaDB
# Requires: docker run -d --name milvus -p 19530:19530 milvusdb/milvus:latest
# pymilvus>=2.4.0                  # Python client for Milvus

# ── Dev/testing utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0              # load .env file for GOOGLE_API_KEY etc.
